#include <iostream>
using namespace std;

int main() {
	// int x, y;
	// cin >> x >> y;
	// cout << "Value of x: " << x << "\nValue of y: " << y;
	
	// string str;
	// getline(cin, str);
	// you will need to use getline(cin,str) when you have to take in a whole string as input
	// as if you only use cin, it will only take all the characters that came before the space, after the space, it requires another variable to store another word
	// so getline easily allows to get a whole string
	// cout << str;

	char ch = 'g';
	// char can store all the characters in the english language and easier and memory efficient when you only need to store 1 character instead of defining whole string
	cout << ch;

	return 0;
}


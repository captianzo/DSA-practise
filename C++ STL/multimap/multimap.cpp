#include <iostream>
using namespace std;

// everything same as map, only difference is that it can store multiple keys
// only mp1[key] cant be used here


// [UNORDERED MAP]
// the difference in map and unorderedMap is the same as set and unorderedSet
// UNQUE elements {no duplicate keys allowed} but not sorted, they're stored randomly
// in almost all cases works in O(1) time and in worst case O(n)

int main() {
	
	return 0;
}